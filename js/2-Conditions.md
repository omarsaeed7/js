# Conditions 
## IF
```js
if(condition)
{}
if(condition)
{}
else
{}
if(condition)
{}
else if{ }
```
## Switch
```js
switch(variable){
    case(value) :
        // -----------
        break  // use break to break if this case applied
    case(value) :
        //-----------
        break 
    default : // this will apply when no case applied
        //---------
}
```
## Use If As Switch
```js
/* This how to use switch like if statement */
/*let age = 30
switch(true){ // this true is a trick to move to cases  
   case (age>20):
      console.log("what is the cost of lies ")
         break
   default:
      console.log("what")
}
*/
```
# Ternary
```js 
console.log(true ? 1 : 2);
// → 1
console.log(false ? 1 : 2);
// → 2
```
